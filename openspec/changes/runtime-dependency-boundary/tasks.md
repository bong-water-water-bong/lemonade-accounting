# Tasks: runtime-dependency-boundary

## Implementation

- [x] Move `lemonade-agents` to an optional `agents` extra.
- [x] Point `lemonade-store` at `main`.
- [x] Add Makefile targets for `.venv` setup and optional agent extras.
- [x] Update README and changelog.

## Verification

- [x] Run `make install`.
- [x] Run `make all`.
- [x] Run `make docs`.
