# Gemini Instructions

Follow `AGENTS.md` first.

Keep accounting deterministic and offline. Never mutate cashier logs,
never add payment processing, and keep runtime dependencies limited to
the repository rules.
